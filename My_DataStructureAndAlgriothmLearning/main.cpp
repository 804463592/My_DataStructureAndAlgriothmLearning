#include<iostream>
#include "my_review.h"
#include "LinearList.h"
#include"Chain.h"

#include<vector>


using namespace std;

int main()
{
	vector<int> ivec = { 1,2 };
	//vector<int> *pivec = new vector<int>({3,2});
	//cout << * (pivec->begin()) << endl;
	ChainNode<int> node;
	double a = 0, b = 1;
	swappp(a, b);

	system("pause");
	return 0;
}
